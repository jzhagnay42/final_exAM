<%= form_with(model: boatcomment, local: true) do |form| %>
  <% if boatcomment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(boatcomment.errors.count, "error") %> prohibited this boatcomment from being saved:</h2>

      <ul>
      <% boatcomment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :boatcomment_name %>
  </div>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_field :comment, id: :boatcomment_comment %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
